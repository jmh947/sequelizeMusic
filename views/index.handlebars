<div class="card" style="width: 30rem;">
  <img src="./assets/img/card.jpg" class="card-img-top" alt="music notes">
  <div class="card-body">
   <div class="box">
  <h2>Search for a new song!</h2>
  <form class="create-form">

    <div class="form-group">
      <label for="song">Song Title:</label>
      <input type="text" id="song" name="title">
    </div>
      <button class="submit">Add Song</button>
  </div>
</div>
{{!-- <div class="box">
  <h2>Search for a new song!</h2>
  <form class="create-form">

    <div class="form-group">
      <label for="song">Song Title:</label>
      <input type="text" id="song" name="title">
    </div>
    <button type="submit">Add Song</button>
    <br>
    <br> --}}

    <div class="box">
      <h3>Songs on Que</h3>

      <ul>
        {{#each song}}
        {{#unless addedPlaylist}}
        {{> music-block}}
        {{/unless}}
        {{/each}}
      </ul>

      <h2>Playlist!</h2>

      <ul>
        {{#each song}}
        {{#if addedPlaylist}}
        {{> music-block}}
        {{/if}}
        {{/each}}
      </ul>


      {{!-- <div class="form-group">
    <label for="listen">Wanna Jam?</label><br>
    <input type="radio" name="listen" value="1" checked> Yes!<br>
    <input type="radio" name="listen" value="0"> Not now.
  </div> --}}

    </div>

  </form>